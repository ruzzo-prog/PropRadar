# n8n
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
GENERIC_TIMEZONE=UTC
# Задайте при включении шифрования учётных данных в n8n (не коммитить реальное значение)
# N8N_ENCRYPTION_KEY=

# Metabase (внутреннее приложение; БД лидов добавляется вручную в UI)
MB_SITE_NAME=PropRadar
JAVA_TIMEZONE=UTC
TZ=UTC

# --- PostgreSQL leads-db (значения для формы «Add database» в Metabase)
# Из контейнера Metabase: propradar-leads-db (container_name) или leads-db (имя сервиса в docker/infra)
LEADS_DB_HOST=propradar-leads-db
LEADS_DB_PORT=5432
LEADS_DB_NAME=leads
LEADS_DB_USER=leads
# Должен совпадать с POSTGRES_PASSWORD из docker/infra локально; не коммитить боевой пароль
LEADS_DB_PASSWORD=changeme

# Evolution API (ключ — только через секреты локально, не в репозитории)
AUTHENTICATION_API_KEY=change-me
SERVER_URL=http://localhost:8080

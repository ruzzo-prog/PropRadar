POSTGRES_USER=leads
POSTGRES_PASSWORD=Melania2012!
POSTGRES_DB=leads

# На сервере: не открывайте 5433 на 0.0.0.0; предпочтительно 127.0.0.1:5433:5432 или без
# публикации порта (только сеть propradar). Временный доступ с дев-машины — SSH-туннель,
# см. docs/DEPLOY_SERVER.md.
